# Copy this file to frontend/.env and fill in your own value.
# Without this, the app falls back to localhost/LAN IP (see data/auth.ts).

# Public URL of the deployed backend (e.g. your Railway domain).
EXPO_PUBLIC_BETHANY_API_URL=
